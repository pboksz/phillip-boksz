.games
  - GAMES.each do |game|
    .game
      .header
        .name= game.name
        .stars= star_rating_tag(game.stars)
      .info
        .section.expansions= game.expansions.join(' | ')
        .section.description= game.description
