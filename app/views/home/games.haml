.games
  - GAMES.each do |game|
    .game
      .header
        .stars= star_rating_tag(game.stars)
        .name= game.name
        .link= icon_link(game.link, 'fas', 'external-link-square-alt', blank: true)
